<%= form_with(model: textfile) do |form| %>
  <% if textfile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(textfile.errors.count, "error") %> prohibited this textfile from being saved:</h2>

      <ul>
        <% textfile.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :file_url %>
    <%= form.text_field :file_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
