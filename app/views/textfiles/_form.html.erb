<%= form_with(model: @textfile, local: true) do |form| %>
   <% if @textfile.errors.any? %>
     <div id="error_explanation">
       <h2><%= pluralize(@textfile.errors.count, "error") %> prohibited this @textfile from being saved:</h2>

       <ul>
         <% @textfile.errors.full_messages.each do |message| %>
           <li><%= message %></li>
         <% end %>
       </ul>
     </div>
   <% end %>

   <div class="field">
     <%= form.label :text_file %>
     <%= form.file_field :text_file %>
   </div>

   <div class="actions">
     <%= form.submit "Upload" %>
   </div>
 <% end %>